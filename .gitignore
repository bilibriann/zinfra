# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# El ejemplo sí se versiona; los .env reales no.
!.env.example

# Brief del proyecto: se mantiene en local, no se versiona.
PROMPT.md

# Fotos originales del proveedor: 349 MB de archivos de cámara. Son la fuente de
# scripts/optimizar-fotos.mjs, no material publicable — todo lo que vive en public/
# se sube al hosting tal cual.
/assets-originales/
